# Copy this file to .env and adjust values for your machine.
# Never commit .env. Change these values before any production deployment.

SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080

DB_HOST=localhost
DB_PORT=5433
DB_NAME=streamflix
DB_USERNAME=streamflix
DB_PASSWORD=streamflix_dev_only
